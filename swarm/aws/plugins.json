{{ source "../common.ikt" }}

{{ $logDir := cat (env "INFRAKIT_HOME") "/logs" | nospace }}

{
    "Plugin" : "instance-aws",
    "Launch" : {
        "os" : {
            "Cmd" : "infrakit-instance-aws --log 5 > {{$logDir}}/instance-aws.log 2>&1"
        }
    }
}
